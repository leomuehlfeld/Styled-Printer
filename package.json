{
  "scripts": {
    "heroku-prebuild": "cd server && npm install",
    "start": "cd server && npm start",
    "migrate": "cd server && npm knex migrate:latest",
    "test": "echo \"testing CLI\""
  }
}
